# Local-dev only: Orchard setup key used to bootstrap the admin account on
# first launch. Do NOT reuse this value outside the Tilt/k3d cluster.
apiVersion: v1
kind: Secret
metadata:
  name: orchard-setup
type: Opaque
stringData:
  setup-key: dev-orchard-setup-key-change-me
