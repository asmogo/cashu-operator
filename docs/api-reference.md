# CashuMint API Reference

_Last updated: 2025-11-01_

This reference documents the `CashuMint` Custom Resource Definition (CRD) provided by the Cashu Mint Operator. It summarizes every spec field, default values, validation rules, and status fields. Refer to [`cashumint_types.go`](../api/v1alpha1/cashumint_types.go) for authoritative definitions generated by Kubebuilder.

---

## 1. Resource Overview

| Field                                      | Description                                                                 |
|-------------------------------------------|-----------------------------------------------------------------------------|
| `apiVersion`                               | `mint.cashu.asmogo.github.io/v1alpha1`                                     |
| `kind`                                     | `CashuMint`                                                                |
| `metadata.name`                            | Unique name of the mint instance (used as resource prefix)                 |
| `metadata.namespace`                       | Kubernetes namespace where resources reside                                |
| `spec`                                     | Desired configuration (see sections below)                                 |
| `status`                                   | Observed state managed by the operator                                     |

### Sample Skeleton

```yaml
apiVersion: mint.cashu.asmogo.github.io/v1alpha1
kind: CashuMint
metadata:
  name: cashumint-sample
spec:
  mintInfo:
    url: https://mint.example.com
  database:
    engine: postgres
    postgres:
      autoProvision: true
  lightning:
    backend: fakewallet
    fakeWallet: {}
```

---

## 2. Spec Fields

### 2.1 Global Settings

| Field                      | Type                                    | Required | Default                   | Validation                                     |
|---------------------------|-----------------------------------------|----------|---------------------------|------------------------------------------------|
| `image`                   | string                                  | No       | `ghcr.io/cashubtc/cdk-mintd:v0.15.0` | —                                      |
| `imagePullPolicy`         | string (`Always`, `Never`, `IfNotPresent`) | No    | `IfNotPresent`            | Enum                                          |
| `imagePullSecrets`        | []`LocalObjectReference`                | No       | —                         | —                                              |
| `replicas`                | int32                                   | No       | 1                         | Min=1, Max=1                                   |
| `resources`               | `corev1.ResourceRequirements`           | No       | CPU `100m-1`, Mem `128Mi-512Mi` (implicit) | Requests ≤ limits enforced via webhook |
| `nodeSelector`            | map[string]string                       | No       | —                         | —                                              |
| `tolerations`             | []`corev1.Toleration`                   | No       | —                         | —                                              |
| `affinity`                | `corev1.Affinity`                       | No       | —                         | —                                              |
| `logging.level`           | string (`trace`..`error`)               | No       | `info`                    | Enum                                           |
| `logging.format`          | string (`json`,`pretty`)                | No       | `json`                    | Enum                                           |
| `storage.size`            | string                                  | No       | `10Gi`                    | —                                              |
| `storage.storageClassName`| string pointer                          | No       | —                         | —                                              |
| `backup`                  | `BackupConfig`                          | No       | disabled                  | S3-compatible backup policy (schedule, retention, destination secrets) |
| `paymentProcessors`       | []`PaymentProcessorSpec`                | No       | —                         | Optional operator-managed payment processor workloads |

### 2.2 Mint Information (`spec.mintInfo`)

| Field                | Type                  | Required | Default      | Validation                                |
|----------------------|-----------------------|----------|--------------|-------------------------------------------|
| `url`                | string                | Yes      | —            | Regex `^https?://.*`                      |
| `listenHost`         | string                | No       | `0.0.0.0`    | —                                         |
| `listenPort`         | int32                 | No       | 8085         | 1 ≤ port ≤ 65535                          |
| `mnemonicSecretRef`  | `SecretKeySelector`   | No       | —            | Secret key must exist in namespace        |
| `name`               | string                | No       | —            | —                                         |
| `description`        | string                | No       | —            | —                                         |
| `descriptionLong`    | string                | No       | —            | —                                         |
| `motd`               | string                | No       | —            | —                                         |
| `pubkeyHex`          | string                | No       | —            | —                                         |
| `iconUrl`            | string                | No       | —            | —                                         |
| `contactEmail`       | string                | No       | —            | —                                         |
| `contactNostrPubkey` | string                | No       | —            | —                                         |
| `tosUrl`             | string                | No       | —            | —                                         |
| `inputFeePpk`        | int32 pointer         | No       | —            | —                                         |
| `enableSwaggerUi`    | bool                  | No       | `false`      | —                                         |

### 2.3 Database (`spec.database`)

| Field                      | Type             | Required | Default   | Validation                                   |
|---------------------------|------------------|----------|-----------|----------------------------------------------|
| `engine`                  | string           | Yes      | `postgres`| Enum: `postgres`, `sqlite`, `redb`            |
| `postgres`                | `PostgresConfig` | Cond.    | —         | Required when engine=`postgres`              |
| `sqlite`                  | `SQLiteConfig`   | Cond.    | —         | Required when engine=`sqlite`                |

#### PostgresConfig

| Field                          | Type                  | Required | Default | Validation                                    |
|--------------------------------|-----------------------|----------|---------|-----------------------------------------------|
| `url`                          | string                | No       | —       | Cannot be set with `urlSecretRef`             |
| `urlSecretRef`                 | `SecretKeySelector`   | No       | —       | Cannot be set with `url`                      |
| `tlsMode`                      | string                | No       | `require` | Enum: `disable`,`prefer`,`require`          |
| `maxConnections`               | int32 pointer         | No       | 20      | Minimum 1                                     |
| `connectionTimeoutSeconds`     | int32 pointer         | No       | 10      | Minimum 1                                     |
| `autoProvision`                | bool                  | No       | `false` | When true, operator provisions DB             |
| `autoProvisionSpec.storageSize`| string                | No       | `10Gi`  | —                                             |
| `autoProvisionSpec.storageClassName` | string pointer | No       | —       | —                                             |
| `autoProvisionSpec.resources`  | `ResourceRequirements`| No       | —       | —                                             |
| `autoProvisionSpec.version`    | string                | No       | `15`    | —                                             |

#### SQLiteConfig

| Field      | Type   | Required | Default | Notes                |
|------------|--------|----------|---------|----------------------|
| `dataDir`  | string | No       | `/data` | Combined with PVC path|

### 2.4 Lightning (`spec.lightning`)

| Field               | Type               | Required | Default | Validation |
|---------------------|--------------------|----------|---------|------------|
| `backend`           | string             | Yes      | —       | Enum: `lnd`, `cln`, `lnbits`, `fakewallet`, `grpcprocessor` |
| `minMint`           | int64 pointer      | No       | —       | —          |
| `maxMint`           | int64 pointer      | No       | —       | —          |
| `minMelt`           | int64 pointer      | No       | —       | —          |
| `maxMelt`           | int64 pointer      | No       | —       | —          |
| Backend-specific struct (one required) | — | Conditional | — | Must be provided based on backend value |

#### LNDConfig

| Field               | Type                | Required | Default | Validation                          |
|---------------------|---------------------|----------|---------|-------------------------------------|
| `address`           | string              | Yes      | —       | Regex `^https?://.*:\d+$`           |
| `macaroonSecretRef` | `SecretKeySelector` | No       | —       | —                                   |
| `certSecretRef`     | `SecretKeySelector` | No       | —       | —                                   |
| `feePercent`        | float pointer       | No       | `0.04`  | —                                   |
| `reserveFeeMin`     | int32 pointer       | No       | `4`     | —                                   |

#### CLNConfig

| Field           | Type          | Required | Default | Validation |
|-----------------|---------------|----------|---------|------------|
| `rpcPath`       | string        | Yes      | —       | —          |
| `feePercent`    | float pointer | No       | `0.04`  | —          |
| `reserveFeeMin` | int32 pointer | No       | `4`     | —          |

#### LNBitsConfig

| Field                    | Type                | Required | Default | Validation          |
|--------------------------|---------------------|----------|---------|---------------------|
| `api`                    | string              | Yes      | —       | Regex `^https?://.*`|
| `adminApiKeySecretRef`   | `SecretKeySelector` | Yes      | —       | —                   |
| `invoiceApiKeySecretRef` | `SecretKeySelector` | Yes      | —       | —                   |
| `retroApi`               | bool                | No       | `false` | —                   |

#### FakeWalletConfig

| Field             | Type          | Required | Default    | Notes             |
|-------------------|---------------|----------|------------|-------------------|
| `supportedUnits`  | []string      | No       | `["sat"]`  | —                 |
| `feePercent`      | float pointer | No       | `0.02`     | —                 |
| `reserveFeeMin`   | int32 pointer | No       | `1`        | —                 |
| `minDelayTime`    | int32 pointer | No       | `1`        | seconds           |
| `maxDelayTime`    | int32 pointer | No       | `3`        | seconds           |

#### GRPCProcessorConfig

| Field          | Type                | Required | Default   | Validation                            |
|----------------|---------------------|----------|-----------|---------------------------------------|
| `processorRef` | string              | No       | —         | Must reference `spec.paymentProcessors[].name` |
| `address`      | string              | Conditional | —      | Required when `processorRef` is not set |
| `port`         | int32               | Conditional | —       | Required when `processorRef` is not set; 1 ≤ port ≤ 65535 |
| `supportedUnits` | []string         | No       | `["sat"]` | —                                     |
| `tlsSecretRef` | `SecretKeySelector` | No       | —         | Secret must provide client.crt/key/ca |

#### PaymentProcessorSpec (`spec.paymentProcessors[]`)

| Field             | Type                           | Required | Default         | Validation |
|------------------|--------------------------------|----------|-----------------|------------|
| `name`           | string                         | Yes      | —               | DNS label format, max 30 chars, unique per mint |
| `image`          | string                         | Yes      | —               | —          |
| `imagePullPolicy`| string (`Always`,`Never`,`IfNotPresent`) | No | `IfNotPresent` | Enum       |
| `replicas`       | int32 pointer                  | No       | `1`             | Min=1      |
| `port`           | int32                          | No       | `50051`         | 1 ≤ port ≤ 65535 |
| `command`        | []string                       | No       | —               | —          |
| `args`           | []string                       | No       | —               | —          |
| `env`            | []`EnvVar`                     | No       | —               | Supports SecretKeyRef values |
| `resources`      | `ResourceRequirements`         | No       | —               | —          |

### 2.5 Optional LDK Node (`spec.ldkNode`)

| Field              | Type          | Required | Default   | Validation                          |
|--------------------|---------------|----------|-----------|-------------------------------------|
| `enabled`          | bool          | Yes      | `false`   | —                                   |
| `feePercent`       | float pointer | No       | `0.04`    | —                                   |
| `reserveFeeMin`    | int32 pointer | No       | `4`       | —                                   |
| `bitcoinNetwork`   | string        | No       | `signet`  | Enum: `mainnet`,`testnet`,`signet`,`regtest` |
| `chainSourceType`  | string        | No       | `esplora` | Enum: `esplora`,`bitcoinrpc`        |
| `esploraUrl`       | string        | No       | —         | —                                   |
| `bitcoinRpc`       | `BitcoinRPCConfig` | No | — | —                                    |
| `storageDirPath`   | string        | No       | —         | —                                   |
| `host`             | string        | No       | `0.0.0.0` | —                                   |
| `port`             | int32         | No       | `8090`    | —                                   |
| `announceAddresses`| []string      | No       | —         | —                                   |
| `gossipSourceType` | string        | No       | `rgs`     | Enum: `p2p`,`rgs`                    |
| `rgsUrl`           | string        | No       | —         | —                                   |
| `webserverHost`    | string        | No       | `127.0.0.1`| —                                  |
| `webserverPort`    | int32         | No       | `8888`    | —                                   |

`BitcoinRPCConfig` requires `host`, `port`, `userSecretRef`, `passwordSecretRef`.

### 2.6 Authentication (`spec.auth`)

| Field                     | Type                 | Required | Default | Notes |
|---------------------------|----------------------|----------|---------|-------|
| `enabled`                 | bool                 | Yes      | `false` | —     |
| `openidDiscovery`         | string               | No       | —       | OIDC discovery endpoint |
| `openidClientId`          | string               | No       | —       | —     |
| `mintMaxBat`              | int32 pointer        | No       | `50`    | —     |
| `enabledMint`             | bool pointer         | No       | `true`  | Toggle operations |
| `enabledMelt`             | bool pointer         | No       | `true`  | —     |
| `enabledSwap`             | bool pointer         | No       | `true`  | —     |
| `enabledCheckMintQuote`   | bool pointer         | No       | `true`  | —     |
| `enabledCheckMeltQuote`   | bool pointer         | No       | `true`  | —     |
| `enabledRestore`          | bool pointer         | No       | `true`  | —     |
| `database.postgres`       | `PostgresConfig`     | No       | —       | Separate auth DB support |

### 2.7 HTTP Cache (`spec.httpCache`)

| Field       | Type            | Required | Default  | Notes                                               |
|-------------|-----------------|----------|----------|-----------------------------------------------------|
| `backend`   | string          | Yes      | `memory` | Enum: `memory`,`redis`                              |
| `ttl`       | int32 pointer   | No       | `60`     | Seconds                                             |
| `tti`       | int32 pointer   | No       | `60`     | Seconds                                             |
| `redis`     | `RedisCacheConfig` | Conditional | — | Required if backend=`redis`              |

`RedisCacheConfig` requires `keyPrefix` and either `connectionString` or `connectionStringSecretRef`.

### 2.8 Management RPC (`spec.managementRPC`)

| Field     | Type  | Required | Default  | Notes |
|-----------|-------|----------|----------|-------|
| `enabled` | bool  | Yes      | `false`  | —     |
| `address` | string| No       | `127.0.0.1` | —  |
| `port`    | int32 | No       | `8086`   | —     |

### 2.9 Ingress (`spec.ingress`)

| Field        | Type          | Required | Default | Notes |
|--------------|---------------|----------|---------|-------|
| `enabled`    | bool          | Yes      | `false` | —     |
| `className`  | string        | No       | `nginx` | —     |
| `host`       | string        | Conditional | —    | Required when enabled |
| `annotations`| map[string]string | No   | —       | Merged with defaults |
| `tls.enabled`| bool          | No       | `true` | Controls TLS block |
| `tls.secretName` | string    | No       | `<name>-tls` | When omitted, generated if cert-manager used |
| `tls.certManager.enabled` | bool | No  | `false` | Enable cert-manager annotations |
| `tls.certManager.issuerName` | string | Conditional | — | Required when enabled |
| `tls.certManager.issuerKind` | string | No | `ClusterIssuer` | Enum: `Issuer`,`ClusterIssuer` |

### 2.10 Service (`spec.service`)

| Field              | Type                | Required | Default       | Notes |
|--------------------|---------------------|----------|---------------|-------|
| `type`             | string              | No       | `ClusterIP`   | Enum: `ClusterIP`,`NodePort`,`LoadBalancer` |
| `annotations`      | map[string]string   | No       | —             | —     |
| `loadBalancerIP`   | string              | No       | —             | For LoadBalancer type only |

### 2.11 Backup (`spec.backup`)

| Field                              | Type                  | Required | Default       | Notes |
|------------------------------------|-----------------------|----------|---------------|-------|
| `enabled`                          | bool                  | Yes      | `false`       | Enables automated backups |
| `schedule`                         | string                | Conditional | `0 */6 * * *` | Required when enabled |
| `retentionCount`                   | int32 pointer         | No       | `14`          | Minimum 1 |
| `s3`                               | `S3BackupConfig`      | Conditional | —          | Required when enabled |
| `s3.bucket`                        | string                | Yes      | —             | Destination bucket |
| `s3.prefix`                        | string                | No       | `<mint-name>` | Object key prefix |
| `s3.region`                        | string                | No       | —             | Region or provider equivalent |
| `s3.endpoint`                      | string                | No       | —             | Optional custom S3-compatible endpoint |
| `s3.accessKeyIdSecretRef`          | `SecretKeySelector`   | Yes      | —             | Secret containing access key ID |
| `s3.secretAccessKeySecretRef`      | `SecretKeySelector`   | Yes      | —             | Secret containing secret access key |

Backups are currently reconciled for auto-provisioned PostgreSQL (`spec.database.postgres.autoProvision=true`).
Restore job scaffolding is triggered with metadata annotations:

- `mint.cashu.asmogo.github.io/restore-object-key`: required S3 object key to restore
- `mint.cashu.asmogo.github.io/restore-request-id`: optional request identifier to force a new restore job name

---

## 3. Status Fields

`status` provides insight into runtime state.

| Field                | Type                    | Description                                              |
|----------------------|-------------------------|----------------------------------------------------------|
| `phase`              | `MintPhase`             | One of `Pending`, `Provisioning`, `Ready`, `Failed`, `Updating` |
| `conditions`         | []`metav1.Condition`    | See [Status Conditions](#status-conditions)              |
| `observedGeneration` | int64                   | Last reconciled spec generation                          |
| `deploymentName`     | string                  | Managed Deployment resource name                         |
| `serviceName`        | string                  | Managed Service name                                     |
| `ingressName`        | string                  | Managed Ingress name                                     |
| `configMapName`      | string                  | Managed ConfigMap name                                   |
| `readyReplicas`      | int32                   | Number of ready mint pods                                |
| `url`                | string                  | Effective access URL (includes HTTPS scheme if TLS)      |
| `databaseStatus`     | `DatabaseStatus`        | Database readiness summary (`connected`, `message`, `lastChecked`) |
| `lightningStatus`    | `LightningStatus`       | Lightning readiness summary (`connected`, `message`, `lastChecked`) |

### Status Conditions

Each condition is represented with `type`, `status`, `reason`, `message`, and `observedGeneration`.

| Condition Type        | Meaning                                                         |
|-----------------------|-----------------------------------------------------------------|
| `Ready`               | Mint deployment is ready to serve traffic                      |
| `DatabaseReady`       | Database dependency gate passed (auto-provisioned Postgres ready or external DB config reconciled) |
| `LightningReady`      | Lightning dependency gate passed after required dependencies are present |
| `ConfigValid`         | Spec validated and rendered configuration applied               |
| `IngressReady`        | Ingress and optional TLS are functional                         |
| `BackupReady`         | Backup CronJob (and optional restore Job trigger) reconciled    |

---

## 4. Defaulting and Validation Summary

- Webhooks ensure required backend sections are present (`lnd`, `cln`, `lnbits`, `fakewallet`, `grpcprocessor`).
- `database.postgres` must include either URL/URL secret or `autoProvision=true`.
- `ingress.host` is required when ingress enabled; cert-manager issuer fields must be set when cert-manager is enabled.
- Resource requests must not exceed limits.
- Default `spec.image` is `ghcr.io/cashubtc/cdk-mintd:v0.15.0`; for PostgreSQL mints using `cdk-mintd:v0.15*`, admission warnings remind operators to take a verified backup before rollout.
- Most optional numeric/string fields have server-side defaults (see sections above).

---

## 5. Complete Example

### Production Example with Auto-Provisioned PostgreSQL, gRPC Processor, cert-manager TLS

```yaml
apiVersion: mint.cashu.asmogo.github.io/v1alpha1
kind: CashuMint
metadata:
  name: cashumint-prod
  namespace: cashu
spec:
  image: ghcr.io/cashubtc/cdk-mintd:v0.15.0
  mintInfo:
    url: https://mint.example.com
    mnemonicSecretRef:
      name: cashumint-prod-secrets
      key: mnemonic
    name: "Example Cashu Mint"
    description: "Production mint"
  database:
    engine: postgres
    postgres:
      autoProvision: true
      autoProvisionSpec:
        storageSize: 50Gi
        resources:
          requests:
            cpu: 500m
            memory: 1Gi
  paymentProcessors:
    - name: spark-primary
      image: ghcr.io/asmogo/cdk-spark-payment-prcoessor:v0.15.0
      port: 50051
    - name: spark-secondary
      image: ghcr.io/asmogo/cdk-spark-payment-prcoessor:v0.15.0
      port: 50051
  lightning:
    backend: grpcprocessor
    grpcProcessor:
      processorRef: spark-primary
      supportedUnits: ["sat"]
      tlsSecretRef:
        name: grpc-client-cert
        key: bundle
  ingress:
    enabled: true
    host: mint.example.com
    tls:
      enabled: true
      certManager:
        enabled: true
        issuerName: letsencrypt-prod
  service:
    type: ClusterIP
  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: 1
      memory: 1Gi
```

---

## 6. Additional Resources

- Generated CRD YAML: [`config/crd/bases/mint.cashu.asmogo.github.io_cashumints.yaml`](../config/crd/bases/mint.cashu.asmogo.github.io_cashumints.yaml)
- Controller logic: [`internal/controller/cashumint_controller.go`](../internal/controller/cashumint_controller.go)
- Deployment planning: [`deployment-guide.md`](deployment-guide.md)
- Troubleshooting: [`troubleshooting.md`](troubleshooting.md)
- Migration: [`migration-guide.md`](migration-guide.md)

Keep this reference aligned with CRD changes. Update version and date whenever the API evolves.
